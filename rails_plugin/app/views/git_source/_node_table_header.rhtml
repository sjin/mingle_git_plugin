<th>Path</th>
<th>Age</th>
<th>Most Recent Committer</th>
<th>Most Recent Changeset</th>
<th>Description</th>


<% javascript_with_rescue do %>
  new Ajax.Request(
    '<%= raw(url_for(:controller => 'git_source', :action => 'load_latest_info')) %>',
    {
      method: 'post',
      parameters: '<%=raw @children.collect(&:last_commit_id).uniq.to_query(:commits) %>'
    }
  );

<% end -%>